{
  "name": "oneround-miniprogram",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "check": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "pnpm run build && jest",
    "watch": "node scripts/build.js --watch"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.5.2",
    "jest-environment-jsdom": "^30.5.2",
    "miniprogram-api-typings": "^4.0.7",
    "miniprogram-simulate": "^1.6.2",
    "ts-jest": "^29.4.14",
    "typescript": "^5.9.3"
  }
}

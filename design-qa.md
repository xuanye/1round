# 战绩页设计验收

final result: passed

## 依据与截图

- 规范：`DESIGN.md`。尺寸、系统字体、无纹理背景、触控尺寸及原生导航优先于图片中的示意样式。
- 参考：`/var/folders/6k/nnrj21_s3yd96drzdp7mm75r0000gn/T/codex-clipboard-9b24b83a-946d-4809-bc04-65b2ce06326d.png`，852 × 1846。
- 环境：微信开发者工具 Stable 2.02.2608070，基础库 3.16.1，真实接口、默认最近半年、前三位牌友。
- 实现截图：`/private/tmp/oneround-performance-qa/390.png`，694 × 1502；设备逻辑尺寸 390 × 844，模拟器显示比例 89%。截图为桌面截图中的设备区域，不是 Canvas 导出的页面图片。
- 全屏并排对照：`/private/tmp/oneround-performance-qa/comparison.png`。两侧统一至 390px 宽；参考高度 845px、实现约 844px，保留原生系统区域，不做非等比拉伸。
- 多尺寸：`/private/tmp/oneround-performance-qa/widths.png`，320、375、390、430px；原始裁剪分别为该目录下的 `320.png`、`375.png`、`390.png`、`430.png`。
- 下部详情：`/private/tmp/oneround-performance-qa/bottom.png`，430px 设备滚动至末行，检查牌友与历史行、箭头、长名称及底部安全区。
- 数据状态说明：参考中的 +460/32/18 等是示例。实现截图为真实战绩 −150/6/2，折线、排名和昵称按接口变化；当前用户真实排名第 4，展开后显示本人高亮。

## 发现与修复记录

1. [P2，已修复] WXML 数字实体被显示成 `&#xf054;` 等文字。改用现有 Font Awesome 字体中的实际字符；后续截图显示正常箭头，时间控件不再被实体文本撑宽。
2. [P2，已修复] 旧 Canvas 接口在高密度屏幕上较模糊，且产生旧接口警告。改为 Canvas 2D，按设备 pixelRatio 设置画布，检查 320/375/390/430px 下的线条、轴标签和末点分值。
3. [P2，已修复] 榜单行的 min-height 加内边距增加了不必要的高度。设置 border-box，将指标外间距调整为标准 16px、图表调整为 150px，最终末行可滚动到达。
4. 初次接口 404 在用户更新后端后消失。真实接口已加载统计与榜单。
5. 热重载期间出现开发者工具的 `routeDone ... webviewId ... not found` 系统错误。切换机型触发完整重载后消失；随后战绩和历史入口均正常，控制台无应用错误，仅保留工具自身的基础库、域名检查及预加载提示。

## 五项视觉检查

- **字体**：中文、数字统一系统无衬线，主标题 26px、区块 20px、正文 16px、辅助信息 13–14px；分值使用等宽数字特性。参考中的衬线数字按 DESIGN.md 明确替换。长名称省略、超长分值缩为 18px 并允许换行。
- **布局**：20px 水平内边距，标准间距变量，连续分区与细分隔线；不使用独立统计卡片。控件至少 44px，历史行至少 72px。可自然滚动，末行与底栏间保留空间。
- **颜色**：纸白背景、森林绿趋势线、陶红正分、灰绿负分、灰色零分，正负号明确。本人的榜单行使用浅绿底；无阴影、渐变或纹理。
- **图片**：头像使用真实用户 URL；缺失时显示昵称首字。参考的三张风景照片属于示例用户资料，不作为产品默认头像。沿用现有底栏图标及字体箭头。
- **内容**：个人统计、趋势、牌友榜、最近两场与参考信息层级一致。本人昵称显示“我”。真实数据与示例不同是预期行为，榜单范围与胜场定义已写入需求文档。

## 交互与验证

- 实机型模拟器检查：320/375/390/430px，标题和指标无横向溢出，底栏保持可见。
- 真实接口加载、牌友展开/收起、本人高亮、全部记录进入历史页、返回战绩页、滚动到最近牌局末行已检查。
- 原生 picker 能打开/关闭；筛选事件与新范围请求由页面测试验证。未在本次 UI 自动化中可靠完成滚轮选项切换，应在真机回归时补查。
- `pnpm run check`、`pnpm test`、`go test ./...`、`git diff --check` 通过。页面测试覆盖加载去重、失败重试、时间范围变化、分值格式和图表边界；后端测试覆盖权限、非法时间、区间边界、排除未参与/进行中牌局、牌友合并、负最高分、并列胜场及空结果。

## 后续检查

- 真机检查原生 picker、网络切换、头像加载和系统字体差异。
- 参考稿比规范字体和触控区域更紧凑；实现按规范允许自然滚动，不保证两条历史记录在所有设备首屏同时出现。
- 截图保存在本机临时目录，长期保留时需另行归档。

## 正负号字号调整

- 累计积分、最高单局、牌友榜及最近牌局的 `+`、`−` 独立渲染，字号为数字的 85%，保持基线对齐；零分不显示符号。
- HUAWEI Mate 70 Pro 模拟器中检查了正负分值和滚动后的最近牌局，符号比例与对齐正常。
- 本次 `pnpm run check`、`pnpm run build`、战绩页 Jest 测试（5 项）及 `git diff --check` 通过。

# Mandatory zero-sum score transfers

All games use score transfers that debit the acting participant and credit one or more selected participants, so every game remains zero-sum by construction. The previous `zeroSumRequired` business option is removed because optional non-zero-sum scoring conflicts with the domain model and makes score history harder to interpret.
